#include <stdio.h>

int main() {

    int age;

    // in settings set the preferences as integrated code terminal
    // Prompt the user to enter their age
    printf("Enter the value: ");
    
    // Ensures the output appears immediately

    // Reads the input value for 'age'
    scanf("%d", &age);

    // Print the entered age
    printf("My age is: %d\n", age);

    return 0;
}

